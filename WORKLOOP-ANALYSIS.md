# Workloop / AgentLoop 完整分析与改进报告

## 结论先行

项目的主要问题不是“流程不够多”，而是流程控制长期偏重状态、文件和 Gate 是否齐全，对 Agent 做判断时使用的前提、候选方案和证据约束不足。这会产生一种稳定的失败模式：形式上完成了流程，结果仍因错误假设、错误路由或自证闭环而偏离真实需求。

本轮已把最高优先级问题落成代码：关键假设进入受控生命周期，关键决策保存候选与证据，开发流程改为由最大风险驱动，并修复无外部 `jsonschema` 环境下的校验回退。

## 项目现状

仓库已经具备较完整的开发控制系统：需求分类、三档执行模式、九类开发流程、三类验证执行器、Gate、Evidence、父子 Loop、集成 Checkpoint、运行时升级以及阶段上下文投影。40 份历史问题档案说明项目持续修补了大量“规则写了但机器未闭环”的漏洞。

这些机制解决了“有没有走流程”，但没有充分解决“为什么做出这个判断”。现有 `classification.basis`、`routing.reason` 等字段都是最终结论式文本，不能区分事实、推测和选择，也无法阻止一个听起来合理但建立在错误前提上的决定。

## 根因分析

### P0：关键假设没有生命周期

需求文档要求区分事实与待确认假设，但 `loop.yaml` 原先没有结构化假设集合。控制程序无法知道哪些假设会影响路由、验收、实现或验证，也无法在假设尚未验证时阻止推进。

结果是：Agent 可以把“应该是”“大概只影响这里”“接口应该兼容”等推测直接写入理由，然后进入编码。失败后只能看到最终路由，看不到最初哪项前提错误。

### P1：决策只保存结果，不保存证据链

原系统保存主流程、置信度和自然语言理由，却不要求列出真实候选、选择依据以及为何排除其他候选。这样无法判断 Agent 是否真的比较过方案，还是先选熟悉路径再补理由。

### P1：流程选择仍可被“名称匹配”驱动

文档已经说开发流程应覆盖最大不确定性，但控制程序只校验 `main_flow` 属于枚举，没有检查它与风险类别的对应关系。分类、流程名和 Agent 习惯都可能代替真正的风险判断。

### P2：控制闭环与上下文投影存在张力

项目已经通过阶段投影缓解上下文膨胀，但新增控制数据若只写入完整底账、不进入相关阶段投影，执行 Agent 仍看不到；若全部无差别投影，又会重新造成膨胀。本轮只投影短小的假设与决策控制数据，保留完整历史在底账。

### 工程质量问题

1. 上传包中的 `README.md` 原本多出一行 `# workloop`，导致发布快照检查失败；这是用户既有工作树修改，本轮没有擅自删除。
2. 根目录 `agentloop/validate_examples.py` 原先硬依赖 `jsonschema`，与插件宣称的自包含目标不一致。
3. 控制程序的回退校验器在常规执行可用，但被动态加载且宿主无 `jsonschema` 时找不到同目录模块，完整生命周期回归失败。

## 已实施修改

### 1. Assumption Tracking

`loop.yaml` 新增 `assumptions`：

- `assumption_id`
- 假设陈述
- 影响类型：非阻塞、路由、验收、实现、验证
- 状态：未验证、已确认、已否定
- 责任人、证据和更新时间

新增 `agentloop assumption` 命令。假设被确认或否定时必须提供证据；存在影响后续工作的未验证假设时，路由命令拒绝推进。

### 2. Decision Evidence

`loop.yaml` 新增 `decision_records`，记录问题、至少两个候选、最终选择、证据、理由、决策人和时间。新增 `agentloop decision` 命令；选择必须属于候选集合，避免只写最终答案。

### 3. 风险驱动路由

`routing` 新增 `risk_driver`，包含风险类别、具体风险、事实依据和严重度。路由命令要求风险类别与主流程严格对应，例如：

- 局部变更 → quick-change
- 用户体验 → product-prototype
- 业务工作流 → business-process
- 数据/契约 → data-contract
- 领域规则 → domain-model
- 系统边界 → architecture
- 根因未知 → root-cause
- 迁移兼容 → migration-compatibility
- 技术可行性 → technical-validation

这样流程成为风险处理策略，而不是需求名称分类器。

### 4. 自包含校验修复

根目录示例校验在没有 `jsonschema` 时改用插件自带轻量校验器；控制程序动态加载时显式加入脚本目录，确保回退模块可解析。

### 5. 协议和问题档案

更新设计原则、路由协议，新增问题档案 041，并同步插件发布快照。

## 验证结果

以下检查全部通过：

- 插件引用同步检查与内容校验
- AgentLoop 完整生命周期回归
- 阶段上下文投影回归
- 集成数据来源 Gate
- 生产原型与控制 Gate
- 原型保真度负向回归
- 全部 project/flow/evidence/loop 示例 Schema 校验
- 无效样例拒绝测试

## 仍需继续的工作

### 下一优先级

1. 给 `assumption` 和 `decision` 命令增加独立负向 CLI 回归，避免未来只被完整生命周期间接覆盖。
2. 为历史活动 Loop 提供显式迁移命令，把新增数组初始化为空，并在首次重新路由时强制补 `risk_driver`。
3. 将风险驱动从单一类别扩展为“主风险 + 次要风险”，但仍只允许一个主流程；次要风险映射为 supporting flow 或验证义务。
4. 增加质量度量：路由前阻塞假设数、被否定假设数、需求版本变更触发的决策失效数、首次验证通过率。
5. 把真实项目运行中的失败记录与假设/决策 ID 关联，验证这套机制是否降低返工，而不只是增加字段。

## Git 发布说明

压缩包包含完整 `.git` 和 `origin git@github.com:kylinmac/workloop.git`。当前执行环境缺少 GitHub CLI，无法安全完成认证、推送和 Draft PR；此外工作树原本已有 `README.md` 与 `.codewhale/` 变更，发布时必须明确排除或由仓库所有者确认是否纳入。
