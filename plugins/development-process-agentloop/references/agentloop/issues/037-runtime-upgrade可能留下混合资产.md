# runtime-upgrade 可能留下混合资产

## 现象
Schema 和示例直接 copytree 覆盖运行目录。
## 影响
中断可能留下新旧文件混合状态。
## 根因
升级没有 staging、校验和目录切换。
## 旧门禁为何没拦截
测试只覆盖正常复制完成。
## 修复
先复制到临时目录并逐文件校验，再通过备份目录切换；中断恢复备份，不保留混合目录。
## 回归
模拟目标缺失但备份存在的中断状态，重跑升级后资产完整且备份清理。
## 预防规则
运行时协议资产升级必须先完整 staging，再整体切换。
