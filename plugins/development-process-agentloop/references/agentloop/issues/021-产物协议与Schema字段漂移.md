# 产物协议与 Schema 字段漂移

## 现象
若干 `files` 字段只存在于 Schema，文件名、字段名和 required output 名称缺少映射。
## 影响
执行方可能按文档写出控制程序不认识的字段。
## 根因
实现新增字段时未同步唯一语义定义。
## 旧门禁为何没拦截
引用同步只复制文件，不检查字段语义覆盖。
## 修复
产物协议列出规范字段、文件名、required output ID 和触发条件。
## 回归
发布引用同步和示例 Schema 校验通过。
## 预防规则
Schema 新增控制字段必须在同一提交更新产物协议。
