# 阶段 1：流程盘点与插件映射

- 状态：completed
- 阶段目标：确认仓库整体流程边界，并将每类规则映射到插件 Skill、参考资料、运行脚本或 Hook。
- 输入与约束：现有需求、开发、验证、Git、AgentLoop 文档与 Schema；插件内不得依赖外部常驻服务。

## 工作项
- [x] 盘点流程文档、Schema、示例和现有校验器
- [x] 明确插件安装包应内置的资料
- [x] 明确运行命令与 Hook 的职责边界

## 交付与验证
- 交付物：插件组件映射和实施范围记录
- 验证方式：仓库中的每类流程资产均有明确插件归属，无演示性空壳

## 执行记录
- 用户已明确要求整体流程插件化；沿用此前“全部完成再停”的授权继续逐阶段执行。
- 完整需求、开发、验证、Git 与 AgentLoop 文档映射为插件 `references/`；Schema 和样例作为插件运行时模板。
- Skill 负责流程选择和角色约束；控制工具负责初始化、Schema、Gate、证据和状态转换；Hooks 负责会话加载、写入门禁和停止检查。
- 阶段验证：所有流程资产均有插件归属，未引入 MCP、App Server 或常驻服务。
