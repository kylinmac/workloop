# Plan：<同 spec 标题>

对应 spec：`spec.md`（requirement 变化时必须先改 spec 再改 plan）

## 风险应对

spec 中的最大风险由 <哪个任务/哪条验证> 覆盖。

## 任务

每个任务：一个上下文内可完成、完成即提交、验证命令真实跑过才勾选。
`证据` 在执行时追加：命令 + 关键输出（或截图路径 / 用户确认原话），必须带日期或紧邻提交 hash——无时间锚的证据 review 时按过期处理。

- [ ] T1（覆盖 AC1）：<做什么>
  - 验证：`<命令>`
  - 证据：
- [ ] T2（覆盖 AC2）：<做什么>
  - 验证：`<命令>`
  - 证据：

## 执行日志

只记录偏离计划的事件：新发现、假设被推翻、任务增删改。正常推进不记录。

- <日期> <事件及处理>
