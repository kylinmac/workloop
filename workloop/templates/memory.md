# 错误记忆（项目级，上限 20 条）

规则见 workloop-memory skill。每条格式固定：**触发条件**（什么信号出现时该想起它）、**教训**（一句话）、**预防检查**（可执行命令优先；没有命令则写 review 校验项）。命中并使用某条时，更新其"最近触发"列（入口 skill 铁律）。

| # | 触发条件 | 教训 | 预防检查 | 来源 loop | 最近触发 |
|---|---|---|---|---|---|
| 1 | <例：改动涉及数据库字段> | <例：只验证写入不验证读回，会漏掉序列化不一致> | <例：验收必须含 readback 命令> | wl-... | wl-...（YYYY-MM-DD） |

当前条数：1 / 20
