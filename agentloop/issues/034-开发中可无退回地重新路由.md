# 开发中可无退回地重新路由

## 现象
`route` 在任意主状态都可执行。
## 影响
开发或验证中可以直接降低验证策略。
## 根因
命令缺少状态前置条件。
## 旧门禁为何没拦截
后续检查只验证新路由是否合法，不验证改变发生的时机。
## 修复
route 仅允许 `ready_for_development/development_preparing`；其他状态必须先合法退回。
## 回归
developing 状态调用 route 被拒绝。
## 预防规则
每个控制命令必须声明并检查所属状态。
