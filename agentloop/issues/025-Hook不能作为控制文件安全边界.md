# Hook 不能作为控制文件安全边界

## 现象
PreToolUse 只匹配一种写工具并放行控制目录。
## 影响
其他写路径不会被前置拦截，文档却容易把 Hook 理解为硬保护。
## 根因
把宿主提示机制和控制程序完整性边界混为一谈。
## 旧门禁为何没拦截
硬校验又存在快照缺失旁路。
## 修复
控制目录不再由 apply_patch Hook 放行；文档明确 Hook 仅减少误操作，每个 CLI 入口仍验证完整性。
## 回归
控制目录 patch 被拒绝；绕过 Hook 后下一个控制命令仍由快照校验拒绝。
## 预防规则
Hook 永不作为唯一安全控制。
