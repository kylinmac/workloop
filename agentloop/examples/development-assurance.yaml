schema_version: 1
loop_id: al-20260731-001
requirement_version: 1
route: root-cause
obligations:
  - obligation_id: reproduction
    scope_id:
    source_obligation_ids: [DEFECT-actual]
    artifact_paths: [development.md]
    checks: [修复前复现命令返回失败]
    gate_ids: []
    recovery: 复现环境不可用时回到 development_preparing 补齐环境
  - obligation_id: failing-regression
    scope_id:
    source_obligation_ids: [DEFECT-expected]
    artifact_paths: [tests/regression.py]
    checks: [修复前失败且修复后通过]
    gate_ids: []
    recovery: 测试不能稳定复现时不得开始编码
